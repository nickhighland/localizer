{
  "name": "localizer",
  "version": "2.0.1",
  "description": "Gives every Docker container on an Unraid server a .local name: reverse proxy, mDNS responder, container discovery and a dashboard.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test 'test/*.test.js'",
    "dev": "CONFIG_DIR=./data HTTP_PORT=8088 node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "private": true
}
